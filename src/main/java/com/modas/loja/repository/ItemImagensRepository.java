package com.modas.loja.repository;

import com.modas.loja.model.ItemImagens;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ItemImagensRepository extends JpaRepository<ItemImagens, Integer> {
}
